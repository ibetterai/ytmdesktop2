{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Allows the main feasibility window to read shell information, query its state, and control only itself.",
  "windows": ["main"],
  "permissions": [
    "allow-tauri-shell-info",
    "allow-tauri-window-control",
    "allow-tauri-main-window-state",
    "allow-tauri-settings-snapshot"
  ]
}
