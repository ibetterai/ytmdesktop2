{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Allows the main feasibility window to read shell information and control only itself.",
  "windows": ["main"],
  "permissions": ["allow-tauri-shell-info", "allow-tauri-window-control"]
}
