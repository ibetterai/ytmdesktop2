{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Allows the main feasibility window to read the typed shell information contract.",
  "windows": ["main"],
  "permissions": ["allow-tauri-shell-info"]
}
