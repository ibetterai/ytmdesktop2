{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Allows the main feasibility window to read shell information, query its state, control only itself, and use the fixed bridge contract.",
  "windows": ["main"],
  "permissions": [
    "allow-tauri-shell-info",
    "allow-tauri-window-control",
    "allow-tauri-main-window-state",
    "allow-tauri-plugin-bridge"
  ]
}
